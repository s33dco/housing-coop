<!-- for meta data -->
<%= title("#{controller_name.titleize}")%>
<%= description("Rent - #{ENV['COOP_FULL_NAME']}")%>
<!--  -->

<article>
	<header>
		<h1><%= ENV['COOP_SHORT_NAME']%> rent administration, <%= pluralize(@rents.size, 'payment') %> received.</h1>
	</header>
	<table>
		<tr>
			<td>Date</td>
			<td>For</td>
			<td>Amount</td>
			<td>Notes</td>
			<td></td>
		</tr>

		<% @rents.each do | rent | %>
		<tr>
			<td><%=short_date(rent.date)%></td>
			<td><%=home_address(rent.property)%></td>
			<td><%=show_money(rent.payment)%></td>		
			<td><%=truncate(rent.notes, length: 40, separator: ' ')%></td>
			<td>updated : <%=short_date(rent.updated_at)%></td>
			<td><%=link_to 'edit', edit_rent_path(rent) %></td>
			<td><%=link_to 'delete', rent, method: :delete, data: { confirm: 'Are you sure?' } %></td>
		</tr>
		<% end %>
	</table>

	<div class="page-actions">
		<%=link_to 'Add A New Rent Payment', new_rent_path %>
	</div>
</article>