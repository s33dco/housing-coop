<!-- for meta data -->
<%= title("#{@property.house_name_no} #{@property.address1}(edit)")%>
<%= description("Edit the property details for #{@property.house_name_no} #{@property.address1} - #{ENV['COOP_FULL_NAME']}")%>
<!--  -->

<header>
	<h1>Editing <%=@property.house_name_no%></h1>
</header>
<%= render 'form_for_property' %>